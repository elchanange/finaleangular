import { BeforeNowPipe } from './before-now.pipe';

describe('BeforeNowPipe', () => {
  it('create an instance', () => {
    const pipe = new BeforeNowPipe();
    expect(pipe).toBeTruthy();
  });
});
